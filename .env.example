# Thermal printer network address
PRINTER_IP=192.168.1.100
PRINTER_PORT=9100

# Gateway HTTP port
SERVER_PORT=3000

# Set to true to skip the real printer and log to console instead
MOCK_PRINTER=false
